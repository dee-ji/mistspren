# Mistspren Simplified Conventions

- Use filename prefixes (e.g., truthwatcher-architecture-...) instead of deep topic subdirs
- YAML frontmatter for metadata
- Delete .gitkeep files after migration
- etc.